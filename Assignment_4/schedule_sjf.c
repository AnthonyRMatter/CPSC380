#include <stdio.h>

#include "schedulers.h"
#include "list.h"

void SJF()
{
    // Take in tasks into an array

    // Call traverse list function in list.c to check the current order of tasks

    // Run a sorting algorithm to sort the array from lowest to highest based on CPU bursts

    // Traverse through list and execute tasks starting at index 0 through end of list    
}